<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>User Dashboard</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background: linear-gradient(to right, #1f4037, #99f2c8);
            font-family: 'Poppins', sans-serif;
            color: #fff;
        }

        .dashboard-card {
            background-color: rgba(255, 255, 255, 0.15);
            border-radius: 20px;
            padding: 30px;
            box-shadow: 0px 5px 25px rgba(0, 0, 0, 0.3);
            backdrop-filter: blur(8px);
        }

        .logout-btn {
            background-color: #dc3545;
            color: white;
            border: none;
            transition: 0.3s;
        }

        .logout-btn:hover {
            background-color: #b02a37;
        }

        .welcome {
            font-weight: 600;
            font-size: 1.5rem;
        }

        .user-info {
            background: rgba(255, 255, 255, 0.2);
            border-radius: 10px;
            padding: 15px;
            color: #fff;
        }
    </style>
</head>

<body>
    <div class="container d-flex justify-content-center align-items-center vh-100">
        <div class="dashboard-card text-center col-md-6 col-sm-10">
            <h2 class="welcome">Welcome, <%= user.name %> 👋</h2>
            <p class="mt-2">You’re logged in successfully.</p>

            <div class="user-info mt-4 text-start">
                <p><strong>Name:</strong> <%= user.name %></p>
                <p><strong>Email:</strong> <%= user.email %></p>
            </div>

            <div class="mt-4">
                <a href="/logout" class="btn logout-btn w-100">Logout</a>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
